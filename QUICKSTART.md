# Быстрый старт

## ✅ Что уже сделано:

1. ✓ Установлены все зависимости (backend и frontend)
2. ✓ Настроена база данных SQLite
3. ✓ Создан .env файл (нужно заполнить реальными данными)
4. ✓ Код проверен и готов к работе

## 🚀 Запуск:

### 1. Заполните Telegram API данные в `backend/.env`:
```bash
# Получите на https://my.telegram.org
TELEGRAM_API_ID=ваш_api_id
TELEGRAM_API_HASH=ваш_api_hash
TELEGRAM_PHONE=+1234567890
ALERT_CHANNEL_ID=@your_channel
```

### 2. Запустите проект:

**Вариант A: Используя скрипт**
```bash
./start.sh
```

**Вариант B: Вручную**

Терминал 1:
```bash
cd backend
npm run dev
```

Терминал 2:
```bash
cd frontend  
npm run dev
```

### 3. Авторизуйтесь в Telegram:

```bash
# Отправить код
curl -X POST http://localhost:3000/api/auth/send-code

# Ввести код (замените 12345 на код из Telegram)
curl -X POST http://localhost:3000/api/auth/verify \
  -H "Content-Type: application/json" \
  -d '{"code":"12345"}'
```

### 4. Откройте админку:
http://localhost:5173

## 📝 Что дальше:

1. Добавьте чаты для мониторинга
2. Добавьте ключевые слова
3. Система автоматически будет мониторить и отправлять алерты

Подробная документация: см. SETUP.md
