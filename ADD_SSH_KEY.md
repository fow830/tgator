# 🔑 Добавление SSH ключа на сервер

## Сгенерированный ключ:

### Публичный ключ (добавьте на сервер):
```
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIJ2YLFjqXJQwsUfji9QFJrINpvfqCiwFILQMq64XmpC3 tgator@betaserver.ru
```

## Инструкция по добавлению:

### 1. Подключитесь к серверу (с паролем):
```bash
ssh root@90.156.229.163
```

### 2. На сервере выполните:
```bash
# Создайте директорию .ssh если её нет
mkdir -p ~/.ssh
chmod 700 ~/.ssh

# Добавьте публичный ключ
echo 'EOF
cat ~/.ssh/id_ed25519_tgator.pub >> ADD_SSH_KEY.md
cat << 'EOF' >> ADD_SSH_KEY.md
' >> ~/.ssh/authorized_keys

# Установите правильные права
chmod 600 ~/.ssh/authorized_keys
```

### 3. Проверьте подключение:
```bash
# С вашего компьютера
ssh -i ~/.ssh/id_ed25519_tgator root@90.156.229.163
```

## После добавления ключа:

Выполните деплой:
```bash
# Добавьте ключ в SSH agent
ssh-add ~/.ssh/id_ed25519_tgator

# Или используйте напрямую
scp -i ~/.ssh/id_ed25519_tgator /tmp/tgator-deploy.tar.gz root@90.156.229.163:/tmp/
scp -i ~/.ssh/id_ed25519_tgator server-deploy.sh root@90.156.229.163:/tmp/
ssh -i ~/.ssh/id_ed25519_tgator root@90.156.229.163 "bash /tmp/server-deploy.sh"
```
